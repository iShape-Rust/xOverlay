pub(crate) struct VrSegm {
    pub(crate) x: i32,
    pub(crate) y_min: i32,
    pub(crate) y_max: i32,
}

pub(crate) struct HzSegm {
    pub(crate) y: i32,
    pub(crate) x_min: i32,
    pub(crate) x_max: i32,
}