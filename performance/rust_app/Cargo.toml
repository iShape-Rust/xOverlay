[package]
name = "rust_app"
version = "0.1.0"
edition = "2021"

[profile.release]
opt-level = 3
lto = false
codegen-units = 1

[dependencies]

#i_overlay = { path = "../../iOverlay", default-features = true }
x_overlay = { path = "../../xOverlay", default-features = false }
