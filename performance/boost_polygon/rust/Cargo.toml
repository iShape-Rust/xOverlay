[package]
name = "overlay_comparison"
version = "0.1.0"
edition = "2024"

[profile.release]
opt-level = 3
codegen-units = 1
lto = false

[dependencies]
x_overlay = { path = "../../../xOverlay" }
i_overlay = { path = "../../../../iOverlay/iOverlay" }
